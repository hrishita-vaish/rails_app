<%= form_for(@postresource) do |f| %>
  <% if @postresource.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@postresource.errors.count, "error") %> prohibited this postresource from being saved:</h2>

      <ul>
      <% @postresource.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
